import SwiftUI

/// The floating readout gets one shared emphasis token so it stays legible over any desktop.
enum DS {
    enum Font {
        static let bodyEmphasis = AppStyle.Font.bodyStrong
    }
}
